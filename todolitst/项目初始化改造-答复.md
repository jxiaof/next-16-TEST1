正在收集工作区信息正在筛选到最相关的信息# Next.js 项目改造计划

根据你的目标，我将帮你一步步将原生 Next.js 项目改造为参考 init.txt 的企业级架构。以下是 MVP 阶段的改造步骤：

## 第一步：创建基础目录结构

首先创建核心目录：

```bash
# 在项目根目录执行
mkdir -p src/backend/{context,model,dao,service,pkg/{config,db,logger}}
mkdir -p src/common/{codes,errors,types,schemas,constants,utils}
mkdir -p tests/{integration,e2e,fixtures}
mkdir -p docs
mkdir -p scripts
mkdir -p infra
mkdir -p deploy/{nginx,docker,cdn}
```